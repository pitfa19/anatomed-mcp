node_modules/
dist/
.env
.env.local
supabasesecret.txt
*.log
.DS_Store
.firecrawl/

# Large binary model assets — sourced from anatomed-web, hosted on Supabase.
# Not committed (keeps the repo light); fetched/uploaded via scripts.
assets/glb/
.vercel

# Local agent/tool state (not part of the project)
.claude/
.serena/

# Local-only paper tooling: hardcodes a private Obsidian vault path, so it must
# never be published with the repo.
scripts/build-paper-pdf.py
